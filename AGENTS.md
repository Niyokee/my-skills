# リポジトリ作業ガイド

## このリポジトリの目的

このリポジトリは、要件定義とドメイン設計を支援する個人用の Codex スキル集です。実行可能なアプリケーションではないため、存在しないビルド手順やテスト手順を追加しないでください。

各スキルは、原則として次の要素で構成します。

- `SKILL.md`: スキルを使う条件、作業手順、出力要件を定義します。
- `references/`: 作業内容に応じて読む詳細な判断基準を格納します。
- `agents/openai.yaml`: Codex 上の表示名、短い説明、既定のプロンプトを定義します。

## 作業前の確認

1. `README.md` と、変更対象の `SKILL.md` を全文読みます。
2. `SKILL.md` が変更対象に応じて読むよう指定している参照文書を全文読みます。
3. `git status --short` で既存の変更を確認します。依頼と無関係な変更や未追跡ファイルは変更しません。
4. 依頼がレビューだけの場合は、対象の成果物を変更せず、根拠、影響、最小限の改善案を報告します。

## `SKILL.md` の規約

- YAML フロントマターには `name` と `description` を記載します。
- `name` は小文字の英数字とハイフンを使います。スキルのディレクトリ名、明示的な呼び出し名 `$<name>`、`agents/openai.yaml` の既定のプロンプトを一致させます。
- `description` には、スキルが行うことと、スキルを使う具体的な条件の両方を日本語で記載します。
- 本文には、作業の目的、必要な参照文書の選択条件、作業手順、出力要件または完了条件を記載します。
- 中核となる手順は `SKILL.md` に残します。詳しい判断基準、長い例、個別の方式に依存する説明は `references/` に分けます。
- 同じ説明を `SKILL.md` と参照文書の両方に重複して記載しません。
- 参照文書へのリンクは、スキルのディレクトリを基準とする相対パスにします。リンクの直前で、いつその文書を読むかを明記します。
- 必要のない `scripts/`、`assets/`、補助的な説明文書は追加しません。

## 参照文書の規約

- 一つの文書には、明確に区切れる一つの論点をまとめます。
- ファイル名には、原則として小文字の英数字とハイフンを使います。
- 既存ファイルを改名する場合は、`SKILL.md` と他の参照文書にある全てのリンクを同じ変更で更新します。
- 業務規則、設計上の判断、具体例、レビュー項目の関係が追跡できる構成にします。
- 型で保証する範囲と、実行時の検証や運用で保証する範囲を区別します。
- 確認済みの事実、設計上の判断、未確認の仮説を区別します。

## `agents/openai.yaml` の規約

- `interface.display_name`、`interface.short_description`、`interface.default_prompt` を記載します。
- 表示内容は `SKILL.md` の目的と適用範囲に一致させます。
- `default_prompt` には `$<name>` を明記し、そのスキルの代表的な依頼を一文で示します。
- `short_description` は、スキル一覧だけを読んでも用途を区別できる具体的な説明にします。

## 日本語と用語

- 説明、手順、既定のプロンプトは日本語で記載します。識別子、コード、定着した技術用語は必要に応じて英語のまま使用できます。
- 略語と専門用語は、各文書の初出時に定義します。
- 同じ概念には、スキルと参照文書を通して同じ名称を使います。
- 「適切に処理する」のように判定できない指示を避けます。確認対象、判断基準、期待する結果を具体的に記載します。
- レビュー用の指示では、問題の根拠、発生し得る不正な状態、推奨する最小限の変更、変更後に残る保証範囲を明記します。

## 文書の同期

スキルを追加、移動、改名、削除した場合は、同じ変更で次の項目を確認します。

- `README.md` の収録スキル一覧、導入方法、使用例、ディレクトリ構成
- `SKILL.md` の `name` と `description`
- `agents/openai.yaml` の表示内容と `$<name>`
- `SKILL.md` と参照文書にある相対リンク

## 検証

変更したスキルごとに、利用可能な場合は Codex に付属するスキル検証プログラムを実行します。

```bash
python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py <変更したスキルのディレクトリ>
```

さらに、次の確認を行います。

1. `git diff --check` を実行し、不要な空白や競合マーカーがないことを確認します。
2. 変更した相対リンクの参照先が存在することを確認します。
3. `SKILL.md`、参照文書、`agents/openai.yaml` を読み直し、適用条件と用語が矛盾していないことを確認します。
4. 自動検証がない変更は、確認した内容を完了報告に記載します。

## 変更の範囲と完了報告

- 依頼を満たす最小限のファイルだけを変更します。
- 既存の表現やファイル名に問題を見つけても、依頼と無関係であれば勝手に修正しません。必要な場合は別の改善案として報告します。
- 新しい依存関係、外部サービス、生成物を追加する前に、その必要性と影響を確認します。
- コミットやプッシュは、明示的に依頼された場合だけ実行します。
- 完了報告には、変更したファイル、変更内容、実行した検証、未実施の検証または未解決事項を記載します。
